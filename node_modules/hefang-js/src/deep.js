"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
var core_1 = require("./core");
var type_1 = require("./type");
function deepClone(obj) {
    return core_1.extend(true, {}, obj);
}
exports.deepClone = deepClone;
/**
 * 深度比较对象是否相等
 * @param obj1
 * @param obj2
 */
function deepEquals(obj1, obj2) {
    if (obj1 === obj2)
        return true;
    if (obj2 == obj1 && obj2 !== obj1)
        return false;
    if (type_1.isEmptyObject(obj1) && type_1.isEmptyObject(obj2))
        return true;
    if (type_1.type(obj1) === "Date" && type_1.type(obj2) === "Date") {
        return obj1.getDate() === obj2.getDate();
    }
    if (type_1.type(obj1) === "RegExp" && type_1.type(obj2) === "RegExp") {
        return obj1 + "" === obj2 + "";
    }
    if (type_1.type(obj1) === "Array" && type_1.type(obj2) === "Array") {
        return obj2.length === obj1.length ? (function (arr1, arr2) {
            for (var i = 0; i < arr1.length; i++) {
                if (!deepEquals(arr1[i], arr2[i]))
                    return false;
            }
            return true;
        })(obj1, obj2) : false;
    }
    for (var key in obj1) {
        if (key in obj2) {
            if (!deepEquals(obj1[key], obj2[key]))
                return false;
        }
        else {
            return false;
        }
    }
    return true;
}
exports.deepEquals = deepEquals;
