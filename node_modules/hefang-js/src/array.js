"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
/**
 * 生成[1,5]的序列数组
 * @param {number} min 开始值
 * @param {number} max 结束值
 * @param {number} step 步长
 */
function range(min, max, step) {
    if (step === void 0) { step = 1; }
    var arr = [];
    step = step || 1;
    for (var i = min; i <= max; i += step) {
        arr.push(i);
    }
    return arr;
}
exports.range = range;
/**
 * 数组扁平化
 * @param {any[]} array 数组
 * @param {number} deep 深度
 */
function flatArray(array, deep) {
    if (deep === void 0) { deep = 1; }
    return "flat" in Array.prototype ? array["flat"](deep) :
        deep > 0 ? array.reduce(function (acc, val) { return acc['concat'](Array.isArray(val) ? flatArray(val, deep - 1) : val); }, [])
            : array.slice();
}
exports.flatArray = flatArray;
