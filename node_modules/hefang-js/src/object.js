"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
var array_1 = require("./array");
/**
 * 链式判断
 * @param {any} obj
 * @param  {...string} attrs
 */
function chainingCheck(obj) {
    var attrs = [];
    for (var _i = 1; _i < arguments.length; _i++) {
        attrs[_i - 1] = arguments[_i];
    }
    if (typeof obj !== "object")
        return obj;
    var attrbutes = array_1.flatArray(attrs.map(function (attr) { return typeof attr === "string" && attr.indexOf('.') !== -1 ? attr.split('.') : attr; }), attrs.length);
    var attr;
    for (var idx = 0; idx < attrbutes.length; idx++) {
        attr = attrbutes[idx];
        if (!attr || !obj || !(attr in obj))
            return null;
        obj = obj[attr];
    }
    return obj;
}
exports.chainingCheck = chainingCheck;
// export function chainingCheck(obj, ...attrs) {
//   if (!_.isObject(obj)) return obj;
//   attrs = _.flattenDeep(attrs.map(attr => (attr || "").indexOf('.') === -1 ? attr : (attr || "").split('.')))
//   let attr;
//   for (let idx = 0; idx < attrs.length; idx++) {
//     attr = attrs[idx];
//     if (!attr) return null;
//     if (!obj || !(attr in obj)) return null;
//     obj = obj[attr]
//   }
//   return obj;
// }
// export function diffObject(obj1: object, obj2: object, options): false | object {
//
// }
