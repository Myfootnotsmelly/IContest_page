export interface Point {
    x: number;
    y: number;
}
export declare type Path = Point[];
/**
 * 判断点在不在圆内
 * @param {Point} point 要判断的点
 * @param {Point} center 圆心
 * @param {number} radius 半径
 * @returns {boolean}
 */
export declare function isPointInCircle(point: Point, center: Point, radius: number): boolean;
/**
 * 判断点在不在路径内
 * @param {Point} point 要判断的点
 * @param {Point[]} path 要断送的路径点
 * @returns {boolean}
 */
export declare function isPointInPath(point: Point, path: Path): boolean;
